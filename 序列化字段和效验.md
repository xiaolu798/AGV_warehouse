MCP 就是 AI 界的“USB 接口”，让你的本地代码和数据能一键插到任何 AI 平台上。
外部客户端	访问后能干什么？
Claude Desktop	你在官方 Claude 聊天框里，直接输入“查一下我数据库里的旅游线路”，它就能调你的代码。
Cursor / VS Code	你写代码时，AI 可以直接读取你本地的 API 文档或运行本地脚本来辅助编程。
Terminal (命令行)	通过像 mcp-get 这样的工具，直接在黑窗口里让 AI 操作你的本地文件。
from pydantic import BaseModel, field_validator, Field
import re

class UserSchemas(BaseModel):
    id: str
    username: str
    phone: str = Field(..., description="手机号") # 必填
    email: str

    # 1. 校验逻辑：检查手机号格式
    @field_validator("phone")
    @classmethod
    def validate_phone(cls, v: str):
        if not re.match(r"^1[3-9]\d{9}$", v):
            raise ValueError("手机号格式不正确")
        return v

    # 2. 转换逻辑：隐藏中间四位 (脱敏)
    @field_validator("phone")
    @classmethod
    def mask_phone(cls, v: str):
        # 如果你希望返回给前端的是隐藏后的手机号
        return f"{v[:3]}****{v[7:]}"

    model_config = {"from_attributes": True}

查询所有的员工
offset:查询数据页码起始下标
关联关系查询和删除
利用prefetch_related('profile'),进行关联字段(一对一,一对多)
stu = await Student.get(id=1).prefetch_related('profile')
stu.profile.address

